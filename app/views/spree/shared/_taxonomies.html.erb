<nav id="taxonomies" class="sidebar-item" data-hook>
  <% get_taxonomies.each do |taxonomy| %>
    <h6 class='taxonomy-root'><%= t(:shop_by_taxonomy => taxonomy.name) %></h6>
    <%= taxons_tree(taxonomy.root, @taxon, 2) %>
  <% end %>
</nav>
